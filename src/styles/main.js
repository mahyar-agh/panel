import { createUseStyles } from "react-jss";

export const styles = createUseStyles({
  mainContainer: {
    marginRight: "1%",
    backgroundColor: "#f8f9fB",
    borderRadius: 30,
    padding: "10px 30px",
  },
});
